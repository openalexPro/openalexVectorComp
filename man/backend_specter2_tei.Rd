% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_core.R
\name{backend_specter2_tei}
\alias{backend_specter2_tei}
\title{Backend preset for a local TEI server serving the merged SPECTER2 proximity model}
\usage{
backend_specter2_tei(
  port = 8080L,
  host = "localhost",
  model = "allenai/specter2_proximity_merged"
)
}
\arguments{
\item{port}{Port that TEI is listening on. Defaults to \code{8080}.}

\item{host}{Host for TEI. Defaults to \code{"localhost"}.}

\item{model}{Provenance label for the served model. Defaults to
\code{"allenai/specter2_proximity_merged"}.}
}
\value{
A backend configuration list compatible with \code{\link[=backend_config]{backend_config()}}.
}
\description{
Convenience wrapper around \code{\link[=backend_config]{backend_config()}} for a SPECTER2 setup served by
a local TEI (text-embeddings-inference) server. The model itself must be
prepared and started externally (see \code{inst/scripts/prepare_specter2_merged.py}
and \code{inst/scripts/start_tei_specter2.sh}, and the \code{specter2-setup} vignette).
}
\details{
The \code{model} argument is metadata only and is recorded in \code{embed_model.yaml}
and parquet partition paths; TEI itself loads whichever model it was started
with.
}
