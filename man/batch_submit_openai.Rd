% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_submit_openai.R
\name{batch_submit_openai}
\alias{batch_submit_openai}
\title{Submit OpenAI Batch jobs for corpus embeddings (asynchronous)}
\usage{
batch_submit_openai(
  project_dir,
  backend = backend_config(provider = "openai"),
  corpus_name = "corpus",
  label = corpus_name,
  batch_size = 5000,
  delete_existing = FALSE,
  text_preprocessor = clean_abstract_for_embedding,
  cleaner_args = list(),
  save_text = TRUE,
  max_requests_per_job = 20000L,
  max_job_bytes = 150 * 1024^2,
  completion_window = "24h",
  verbose = TRUE
)
}
\arguments{
\item{project_dir}{Project root directory.}

\item{backend}{Backend configuration from \code{\link[=backend_config]{backend_config()}}. Must
use \code{provider = "openai"}.}

\item{corpus_name}{Folder name under \code{project_dir} containing the corpus
dataset. Defaults to \code{"corpus"}.}

\item{label}{Embedding label partition. Defaults to \code{corpus_name}.}

\item{batch_size}{Number of corpus rows per Arrow scan batch while preparing
requests.}

\item{delete_existing}{If \code{TRUE}, remove existing embeddings for \code{label} and
existing OpenAI batch state before submitting new jobs.}

\item{text_preprocessor}{Text-preparation function returning \code{id}, \code{text},
\code{text_hash}.}

\item{cleaner_args}{Additional named arguments passed to \code{text_preprocessor}.}

\item{save_text}{Logical; whether to keep cleaned text for downstream parquet
output.}

\item{max_requests_per_job}{Max requests per submitted OpenAI job. Must be
<= 50000.}

\item{max_job_bytes}{Max JSONL bytes per submitted OpenAI job. Must be
<= 200 MB.}

\item{completion_window}{OpenAI batch completion window. Defaults to \code{"24h"}.}

\item{verbose}{Logical; print progress messages.}
}
\value{
Invisibly returns a list with state path and submission summary.
}
\description{
Preprocesses corpus text, performs preflight request-size checks, splits work
into compliant OpenAI batch jobs, submits them, and returns immediately.
}
