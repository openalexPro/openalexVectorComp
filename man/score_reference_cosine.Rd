% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_reference_cosine.R
\name{score_reference_cosine}
\alias{score_reference_cosine}
\title{Convert reference-cosine distances to scores}
\usage{
score_reference_cosine(
  distance_parquet,
  output_dir = NULL,
  method = c("linear", "exponential"),
  alpha = 1,
  verbose = TRUE
)
}
\arguments{
\item{distance_parquet}{Path to a Parquet dataset (file or directory) with
first column \code{id} and one or more numeric distance columns.}

\item{output_dir}{Optional output directory. If \code{NULL}, defaults to replacing
\code{"distance_reference_cosine"} with \code{"score_reference_cosine"} in
\code{distance_parquet}.}

\item{method}{Scoring transform: \code{"linear"} (default, \code{1 - distance}) or
\code{"exponential"} (\code{exp(-alpha * distance)}).}

\item{alpha}{Positive numeric scaling factor used when
\code{method = "exponential"}.}

\item{verbose}{Logical; print progress messages.}
}
\value{
Invisibly returns output directory.
}
\description{
Reads a wide reference-cosine distance matrix (as written by
\code{\link[=distance_reference_cosine]{distance_reference_cosine()}}) and converts all numeric distance columns to
scores.
}
