% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tei_server.R
\name{tei_start}
\alias{tei_start}
\title{Start a local TEI server (background)}
\usage{
tei_start(
  model = "sentence-transformers/all-MiniLM-L6-v2",
  port = 3000L,
  bin = NULL,
  args = character(),
  timeout = 90,
  verbose = TRUE
)
}
\arguments{
\item{model}{Model id to load (passed to \code{--model-id}).
Default \code{"sentence-transformers/all-MiniLM-L6-v2"}.}

\item{port}{TCP port to bind. Default \code{3000}.}

\item{bin}{Path to the \code{text-embeddings-router} binary. When \code{NULL}, common
locations are checked (including \code{Sys.which("text-embeddings-router")}).}

\item{args}{Additional CLI flags to pass to the router (character vector).
For example: \code{c("--normalize", "--pooling", "mean")}.}

\item{timeout}{Seconds to wait for the server to report healthy. Default \code{90}.}

\item{verbose}{Print progress messages. Default \code{TRUE}.}
}
\value{
A list with \code{pid}, \code{port}, \code{url}, and \code{cmd} (invisibly).
}
\description{
Spawns a \code{text-embeddings-router} process in the background and waits until
the \verb{/health} endpoint is reachable. The process PID is cached inside the
package and can be stopped with \code{\link[=tei_stop]{tei_stop()}}. If a server already appears to
be running at the same port, the function returns immediately.
}
