% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_abstract_for_embedding.R
\name{clean_abstract_for_embedding}
\alias{clean_abstract_for_embedding}
\title{Clean title/abstract rows into embedding-ready text}
\usage{
clean_abstract_for_embedding(
  df,
  mode = c("lenient", "balanced", "strict"),
  no_abstract_policy = c("keep_title_only", "discard", "conditional"),
  min_chars = NULL,
  min_alpha_ratio = NULL,
  placeholder_patterns = NULL,
  boilerplate_patterns = NULL,
  html_patterns = NULL,
  return_flags = TRUE
)
}
\arguments{
\item{df}{Data frame with columns \code{id}, \code{title}, and \code{abstract}.}

\item{mode}{Cleaning intensity: \code{"lenient"}, \code{"balanced"} (default), or
\code{"strict"}.}

\item{no_abstract_policy}{Policy when abstract is missing/invalid:
\code{"keep_title_only"} (default), \code{"discard"}, or \code{"conditional"}.}

\item{min_chars}{Optional minimum abstract length in characters after
cleaning. If \code{NULL}, mode-specific defaults are used.}

\item{min_alpha_ratio}{Optional minimum ratio of alphabetic characters in the
cleaned abstract. If \code{NULL}, mode-specific defaults are used.}

\item{placeholder_patterns}{Optional regex vector for placeholder abstract
detection.}

\item{boilerplate_patterns}{Optional regex vector for publisher boilerplate
detection.}

\item{html_patterns}{Optional regex vector for HTML/XML artifact detection.}

\item{return_flags}{If \code{TRUE}, include provenance/quality columns.}
}
\value{
A data frame with at least columns \code{id}, \code{text}, \code{text_hash}.
When \code{return_flags = TRUE}, also includes \code{text_quality},
\code{abstract_raw_present}, \code{abstract_kept}, \code{discard_reason}, and
\code{cleaning_mode}.
}
\description{
Applies lightweight rule-based cleaning to \code{title}/\code{abstract} rows and
returns embedding-ready text plus a deterministic \code{text_hash}.
}
