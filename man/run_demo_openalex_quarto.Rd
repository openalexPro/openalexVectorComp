% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_demo_openalex_quarto.R
\name{run_demo_openalex_quarto}
\alias{run_demo_openalex_quarto}
\title{Create and optionally run a self-contained demo project via Quarto}
\usage{
run_demo_openalex_quarto(
  demo_dir = file.path(getwd(), "demo_project"),
  render = TRUE,
  backend = NULL,
  max_corpus = 100,
  max_reference = 10,
  overwrite = FALSE,
  quarto_file = "openalex_demo_analysis.qmd",
  verbose = TRUE
)
}
\arguments{
\item{demo_dir}{Demo workspace directory. Defaults to
\code{file.path(getwd(), "demo_project")}.}

\item{render}{Logical; if \code{TRUE} (default), run \verb{quarto render} on the
copied template.}

\item{backend}{Optional backend config from \code{\link[=embedding_backend_config]{embedding_backend_config()}}. If
\code{NULL}, defaults to Hugging Face (\code{provider = "hf"},
model \code{"BAAI/bge-small-en-v1.5"}).}

\item{max_corpus}{Maximum number of corpus fixture rows to copy.}

\item{max_reference}{Maximum number of reference fixture rows to copy.}

\item{overwrite}{Logical; if \code{FALSE} (default), stop when demo-managed files
already exist. If \code{TRUE}, refresh demo-managed files.}

\item{quarto_file}{Name of the analysis file created in \code{demo_dir}.}

\item{verbose}{Logical; print progress messages.}
}
\value{
Invisibly returns a list with project paths and render status.
}
\description{
Sets up a demo workspace under \code{demo_dir}, creates a pipeline project under
\code{demo_dir/project}, copies small corpus and
reference fixtures and Quarto template from \code{inst/ovc_demo}, and optionally
renders the analysis.
}
\details{
The workspace keeps all generated directories and output artifacts. The
Quarto file is created in \code{demo_dir}, while embedding pipeline data is stored
in \code{demo_dir/project}.
}
