% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance_reference_cosine.R
\name{distance_reference_cosine}
\alias{distance_reference_cosine}
\title{Pairwise cosine distances with centroid axis between label partitions}
\usage{
distance_reference_cosine(
  project_dir,
  embeddings_dir = "model_id=BAAI_bge-small-en-v1.5",
  corpus_label = "corpus",
  reference_label = "reference",
  batch_size = 1e+05,
  max_cells = 5e+07,
  verbose = TRUE
)
}
\arguments{
\item{project_dir}{Project root directory containing \verb{embeddings/}.}

\item{embeddings_dir}{Model subfolder under \code{project_dir/embeddings}, e.g.
\code{"model_id=BAAI_bge-small-en-v1.5"}.}

\item{corpus_label}{Label partition used as corpus side. Defaults to
\code{"corpus"}.}

\item{reference_label}{Label partition used as reference side. Defaults to
\code{"reference"}.}

\item{batch_size}{Unused placeholder for compatibility with planned streaming
extension.}

\item{max_cells}{Maximum allowed matrix size
(\code{(n_corpus + 1) * (n_reference + 1)}) to guard memory use.}

\item{verbose}{Logical; print progress messages.}
}
\value{
Invisibly the output directory
\verb{project_dir/distance_reference_cosine/model_id=<...>/corpus_label=<...>/reference_label=<...>/}.
}
\description{
Reads embeddings from a model-specific dataset and computes cosine distances
between all vectors in \code{corpus_label} and all vectors in \code{reference_label}.
A centroid row/column is added to the matrix:
\itemize{
\item rows are corpus ids plus \code{"centroid"} (corpus centroid),
\item columns are reference ids plus \code{"centroid"} (reference centroid).
}
}
\details{
Embeddings are expected under:
\verb{project_dir/embeddings/model_id=<...>/label=<label>/batch=<n>/...}

Output file:
\itemize{
\item \code{pairwise-cosine.parquet}: wide table with first column \code{id} (corpus id or
\code{"centroid"}), reference-id columns, and a final \code{centroid} column.
}
}
