% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_ridge.R
\name{fit_ridge}
\alias{fit_ridge}
\title{Cross-validated ridge logistic regression using glmnet}
\usage{
fit_ridge(X, y)
}
\arguments{
\item{X}{Numeric feature matrix (`n x d`).}

\item{y}{Binary response vector (`0/1`).}
}
\value{
A `cv.glmnet` fit object.
}
\description{
Wraps `glmnet::cv.glmnet()` with `alpha = 0` for ridge regularisation. The
function coerces labels to numeric and returns the `cv.glmnet` object.
}
\examples{
set.seed(1)
X <- matrix(rnorm(30), nrow = 10)
y <- sample(0:1, 10, replace = TRUE)
fit <- fit_ridge(X, y)
fit$lambda.min

}
