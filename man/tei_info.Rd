% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tei_server.R
\name{tei_info}
\alias{tei_info}
\title{Inspect TEI backend, model, and limits of a running server}
\usage{
tei_info(tei_url = tei_default_embed_url(), timeout = 10, yaml_path = NULL)
}
\arguments{
\item{tei_url}{URL to the running server's \verb{/embed} endpoint. Defaults to the
URL remembered by \code{\link[=tei_start]{tei_start()}} for this session (if available).}

\item{timeout}{Seconds to wait for HTTP requests (per call). Default \code{10}.}

\item{yaml_path}{Optional path to write a YAML report; if \code{NULL}, only returns
the list.}
}
\value{
A named list with fields \code{started} (reachability), \code{backend},
\code{device}, \code{dtype}, \code{model} (nested), \code{server} (nested), and \code{probe} (nested).
}
\description{
Queries an already running TEI service via \verb{/health}, \verb{/info}, and performs
a tiny \verb{/embed} probe to infer embedding dimension and latency. No processes
are launched by this function.
}
