% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed_corpus.R
\name{embed_corpus}
\alias{embed_corpus}
\title{Stream a corpus dataset, embed in batches, and write Parquets}
\usage{
embed_corpus(
  project_dir = NULL,
  backend = backend_config(),
  corpus_name = "corpus",
  batch_size = 5000,
  delete_existing = FALSE,
  text_preprocessor = clean_abstract_for_embedding,
  cleaner_args = list(),
  save_text = TRUE,
  label = corpus_name,
  dry_run = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{project_dir}{Project root directory. Must contain
\verb{project_dir/<corpus_name>} with columns \code{id}, \code{title}, \code{abstract}.}

\item{backend}{Backend configuration created with
\code{\link[=backend_config]{backend_config()}}.}

\item{corpus_name}{Folder name under \code{project_dir} containing the corpus
parquet dataset. Defaults to \code{"corpus"}.}

\item{batch_size}{Number of corpus rows per Arrow scan batch.}

\item{delete_existing}{If \code{TRUE}, old embeddings for the target model are
deleted before processing. If \code{FALSE}, unchanged rows are skipped using
\code{id + text_hash}.}

\item{text_preprocessor}{Function that prepares embedding text from a batch
data frame and returns at least columns \code{id}, \code{text}, \code{text_hash}.
Defaults to \code{\link[=clean_abstract_for_embedding]{clean_abstract_for_embedding()}}.}

\item{cleaner_args}{Named list of additional arguments passed to
\code{text_preprocessor}.}

\item{save_text}{Logical; if \code{TRUE} (default), store the cleaned embedding
text in output Parquet files as column \code{text}. If \code{FALSE}, only \code{text_hash}
is stored.}

\item{label}{Partition label written under
\verb{project_dir/embeddings/model_id=<...>/label=<label>/batch=<n>/}.
Defaults to \code{corpus_name}.}

\item{dry_run}{Logical; if \code{TRUE}, run preprocessing and unchanged-row
filtering without requesting embeddings or writing output files. In this
mode, a Parquet preview file is written to
\verb{project_dir/<corpus_name>_dryrun.parquet}.}

\item{verbose}{Logical; print progress and summary messages.}
}
\value{
Invisibly the model-specific embeddings directory under
\verb{project_dir/embeddings/model_id=<...>/}.
}
\description{
Processes a Parquet dataset without loading it fully in memory. Reads Arrow
record batches, builds canonical text from \code{title} + \code{abstract}, calls the
configured embedding backend, and writes Parquet batch files.
}
