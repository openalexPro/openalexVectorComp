% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_ridge.R
\name{score_ridge}
\alias{score_ridge}
\title{Convert ridge distances to ridge scores}
\usage{
score_ridge(distance_parquet, output_dir = NULL, alpha = 0.5, verbose = TRUE)
}
\arguments{
\item{distance_parquet}{Path to a Parquet dataset (file or directory) with
at least columns \code{id} and \code{area_distance}.}

\item{output_dir}{Optional output directory. If \code{NULL}, defaults to replacing
\code{"distance_ridge"} with \code{"score_ridge"} in \code{distance_parquet}.}

\item{alpha}{Positive numeric scaling factor in
\code{exp(-alpha * area_distance)}. Default \code{0.5} reproduces previous behavior.}

\item{verbose}{Logical; print progress messages.}
}
\value{
Invisibly returns output directory.
}
\description{
Reads a distance dataset with columns \code{id} and \code{area_distance}, computes
\code{relevance_score = exp(-alpha * area_distance)}, and writes a scored Parquet
dataset.
}
